# かんたんセットアップ（スマホ用アプリを一気に入れる）

> このアプリは **常時起動するホスト**（自宅の Mac / Windows PC / Raspberry Pi）で
> サーバを動かし、**スマホはその画面をアプリとして使う**構成です。
> スマホ本体には Python は不要で、**QR を撮って「ホーム画面に追加」するだけ**です。

## 1. ホスト側（PC / Raspberry Pi）でセットアップ

配布 zip を展開し、Sonos と同じ Wi‑Fi につないだうえで：

**macOS / Linux / Raspberry Pi**
```bash
bash install.sh      # ./install.sh でも可(実行権限があれば)
```

**Windows（PowerShell）**
```powershell
powershell -ExecutionPolicy Bypass -File install.ps1
```

初回だけ依存インストール→自動でサーバが起動します（2 回目以降は
`./install.sh` を再実行、または `.venv` のまま `serve` を実行）。

## 2. スマホに“アプリ”を入れる（一気に）

起動すると **QR 画像がホスト側のブラウザで自動で開きます**（`data/qr.html`）。
ターミナルにも QR が表示されますが、文字化けする環境ではブラウザの QR 画像を使ってください。

1. スマホの**カメラで QR を読み取る**（URL にトークンが含まれ、認証も自動で通ります）
   - QR が読めない/開かないときは、表示された `http://<PCのIP>:8765/?token=...` を
     スマホのブラウザに直接入力しても同じです。
2. ブラウザで開いたら **ホーム画面に追加**
   - iPhone(Safari): 共有 → 「ホーム画面に追加」
   - Android(Chrome): メニュー → 「ホーム画面に追加」
3. ホーム画面の **「睡眠BGM」アイコン**から全画面アプリとして起動

## 3. 使い始める

1. 右上で部屋 `主書斎` を選ぶ
2. 「＋ 新しいセット」→ 時刻・スリープタイマー・プレイリストを設定して保存
3. 以降は毎日その時刻に自動再生（サーバを起動したままにしてください）

---

## 🌏 外出先からも使う（オプション・無料）

そのままでは自宅 Wi-Fi 内でしか使えません。**外出先（モバイル回線）からも**
スケジュールの変更や再生をしたい場合は、[Tailscale](https://tailscale.com/)
（個人利用無料の VPN）を入れるだけで対応できます。アプリ側は**自動で検出**します。

1. **ホスト（サーバを動かす PC / Raspberry Pi）に Tailscale をインストール**してログイン
   - https://tailscale.com/download （Google/Apple/GitHub アカウントで OK）
2. **スマホにも Tailscale アプリ**を入れて**同じアカウント**でログイン（VPN を ON）
3. サーバを再起動（`serve`）→ QR ページに
   **「📶 外出先でも使える (Tailscale・推奨)」の QR** が追加で表示される
4. その QR をスキャンして**ホーム画面に追加**し直す

Tailscale の URL は**自宅でも外でも同じ**なので、以後はこちらを常用してください
（自宅 Wi-Fi 用のアイコンは削除して OK）。通信は WireGuard で暗号化され、
あなたのアカウントの端末以外からは一切届きません。

> ⚠️ **ルータのポート開放（ポートフォワーディング）はしないでください。**
> 操作 API が全インターネットに露出します。外部アクセスは必ず Tailscale
> などの VPN 経由にしてください。

### ずっと動かしておくには
PC/Raspberry Pi を消しても自動起動するよう、`README.md` の systemd 設定を参照してください。

### QR が読めないときは
表示された `http://<PCのIP>:8765/?token=...` をスマホのブラウザに直接入力しても同じです。
