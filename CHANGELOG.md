# 変更履歴（CHANGELOG）

配布 zip の版ごとの差分を記録します。各版の詳細な差分はコミットハッシュで
`git show <hash>` / `git diff <前版>..<今版>` から確認できます。

---

## v0.8 — SwitchBot 連携（2026-07-11）

**前版 (v0.7) からの差分:**

- 🔌 **SwitchBot 製品と組み合わせ可能に**。セットごとに SwitchBot デバイスの
  ON/OFF 操作を複数紐づけられる（例: 21:00 の BGM 開始と同時にエアコン OFF）
- ⏱ 操作タイミングを選択可: **「再生開始時」/「スリープ終了時」**
  （スリープ終了時 = BGM 停止と同時に家電も止まる。遅延実行で実現）
- ⚙ **設定モーダル**を新設し、SwitchBot API v1.1 のトークン/シークレットを登録
  （API へは秘密情報を返さない。`switchbot_configured` フラグのみ公開）
- 📡 **デバイスピッカー**: SwitchBot API からエアコン等の赤外線リモコンと
  物理デバイスを取得し、「OFF にする / ON にする」の大ボタンで追加
- 実装: `switchbot.py` 新設（標準ライブラリのみで HMAC-SHA256 署名・HTTP）。
  家電操作は再生と独立にベストエフォート実行（失敗してもBGMは流れる）
- テスト 16 件追加（署名/一覧/タイミング/秘匿/API、計 85 件パス）
- 変更: `models.py` / `switchbot.py`(新規) / `webapp.py` / `scheduler.py` /
  `static/*` / docs / tests

## v0.7 — 外部アクセス対応（2026-07-11）`5232bd3`

**前版 (v0.6) からの差分:**

- 🌏 **外出先（モバイル回線）からも使えるように対応**。ホストに
  [Tailscale](https://tailscale.com/)（個人無料 VPN）が入っていれば `serve` が
  **自動検出**し、外出先用 URL と QR を追加表示（`cli.py: _tailscale_ip()`）
- QR ページ（`data/qr.html`）が複数 QR の並列表示に対応
  （「📶 外出先でも使える (Tailscale・推奨)」「自宅 Wi-Fi 用」）（`qr.py`）
- QUICKSTART に Tailscale 導入手順（ホスト＋スマホ）と
  **ポート開放禁止**の警告を追記
- テスト追加: Tailscale 検出 5 件・複数 QR 1 件（計 69 件パス）
- 変更ファイル: `cli.py` / `qr.py` / `QUICKSTART.md` / `README.md` / tests
  （+151 / −19 行）

## v0.6 — UI 改善＋部屋設定バグ修正（2026-07-01）`a9164c0`

- 🎵 **BGM 選択をボタン化**。タップで全画面ピッカー（検索付き・アイコン付き
  大ボタン行）が開き、タップ＝選択確定で自動的に閉じる
- ⏱ スリープタイマーをプルダウン → **チップボタン**（無効/15/30/45/60/90/120/その他）に変更
- 🐛 **「部屋を設定したはずなのに未設定と言われる」バグを修正**
  - ドロップダウンが未保存でも部屋名を表示してしまう「見た目と保存状態のズレ」を排除
    （未設定時はプレースホルダ表示＋部屋 1 つなら自動設定）
  - 空文字 room の PUT が設定済みの部屋を上書きする経路をクライアント/サーバ両方で遮断

## v0.5 — QR 表示の端末非依存化（2026-07-01）`872cb91`

- 🐛 Windows コンソールで QR が表示されない問題を修正。QR を SVG で
  `data/qr.html` に書き出し**ブラウザで自動オープン**する方式へ（`qr.py` 新設）

## v0.4 — Windows インストーラ修正（2026-07-01）`9dec82f`

- 🐛 `install.ps1` が日本語版 Windows の PowerShell 5.1 でパースエラーに
  なる問題を修正（BOM 付き UTF-8 + CRLF で保存）

## v0.3 — ワンコマンド導入＋QR（2026-07-01）`87a5d89`〜`820a685`

- 📦 `install.sh` / `install.ps1`（仮想環境作成→依存導入→起動をワンコマンド化）
- 📱 起動時にスマホ用 URL（トークン付き）の QR を表示
- 📄 `QUICKSTART.md` 追加

## v0.2.1 — セキュリティ監査と修正（2026-07-01）`9de1051`〜`66970aa`

- 🔐 **API トークン認証**（`data/auth_token` 自動生成・定数時間比較・401）
- 🔐 セキュリティヘッダ（CSP / nosniff / frame-ancestors）と `/api` の no-store
- 🔐 URI 音源のスキーム検証（javascript:/file:/data: 等を拒否）
- 🐛 不正 timezone 保存でサーバ起動不能になる問題を修正
- 🐛 シングルスレッド起動＋同期フェードで全リクエストが最大 30 秒
  ブロックされる問題を修正
- 🐛 数値フィールドの型未検証（文字列で 500、bool のすり抜け）を修正
- 📄 監査レポート `docs/AUDIT_2026-07-01.md`

## v0.2 — スマホアプリ化（PWA）（2026-06-28）`6755e5a`

- 📱 ホーム画面に追加してネイティブ風に起動できる PWA 化
  （manifest / Service Worker / アイコン / セーフエリア対応）
- スマホ向けレスポンシブ UI、`serve` の LAN 公開と URL 表示

## v0.1.1 — Web アプリ化（2026-06-28）`5dd5483`

- Sonos アプリ風 Web UI（プレイリスト/お気に入りの一覧・検索・選択）
- 「時刻 × BGM」セットの複数ストック（有効/無効・編集・削除・即時再生）
- スリープタイマー（既定 60 分/無効/任意）、JSON 永続化、REST API

## v0.1 — 初版（2026-06-28）`da8723f`

- CLI ベースの毎日定時再生スケジューラ（SoCo + APScheduler）
